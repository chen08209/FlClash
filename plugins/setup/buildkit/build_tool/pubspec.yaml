name: build_tool
description: FlClash build tool — compiles Go core and Rust helper for all platforms.
publish_to: none
version: 1.0.0

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  args: ^2.4.2
  logging: ^1.2.0
  path: ^1.9.0
  yaml: ^3.1.2
  crypto: ^3.0.3

dev_dependencies:
  lints: ^2.1.0
  test: ^1.24.0
